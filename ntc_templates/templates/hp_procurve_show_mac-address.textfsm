Value MAC_ADDRESS ([0-9a-fA-F]{6}-[0-9a-fA-F]{6})
Value PORT (\S+)
Value VLAN (\d+)

Start
  ^\s*--- -> Start_record

Start_record
  ^\s+${MAC_ADDRESS}\s+${PORT}\s+${VLAN} -> Record
  ^\s+${MAC_ADDRESS}\s+${PORT} -> Record
  ^\s*$$
  ^. -> Error
