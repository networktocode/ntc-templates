Value Required VLAN_ID (\d+)
Value NAME (.*)
Value STATUS (\S+)
Value INTERFACES (\S+)

Start
  ^\s+\{
  ^\s+"vlanshowbr-vlanid":\s+"${VLAN_ID}"
  ^\s+"vlanshowbr-vlanname":\s+"${NAME}"
  ^\s+"vlanshowbr-vlanstate":\s+"${STATUS}"
  ^\s+"vlanshowplist-ifidx":\s+"${INTERFACES}"
  ^\s+\},? -> Record
