Value Required NODE (.+)
Value ACTIVE_PACKAGES (\d+)
Value List PACKAGES (.+)


Start
  ^.+UTC -> Nd
 
Nd
  ^\s+Node\s+${NODE}
  ^\s+Active Packages:\s+${ACTIVE_PACKAGES} ->  Pack
  
Pack  
  ^\s+${PACKAGES}
  ^\S? -> Record Nd